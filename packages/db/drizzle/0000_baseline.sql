-- Schema already exists (Prisma history archived under docs/drizzle-migration/archived-prisma).
-- This entry only anchors the Drizzle Kit snapshot so `drizzle-kit generate` diffs correctly.
-- Do not recreate tables. Cloudflare Hyperdrive Drizzle docs: skip migrate when schema already matches.
SELECT 1;
